export const DEFAULT_CHUNK_SIZE_BYTES = 5 * 1024 * 1024; // 5MB chunks for multipart
export const MAX_PARALLEL_UPLOADS = 3;
export const RECORDING_MIME_TYPE = "video/webm;codecs=vp9,opus";
export const UPLOAD_PART_SIZE_BYTES = 10 * 1024 * 1024; // 10MB parts for resiliency
